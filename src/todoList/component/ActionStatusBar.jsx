import React from "react"
import "../stylesheet/ActionStatusBar.scss"

const ActionStatusBar = (props) => {
    return (
        <div id="action-status-bar">
            test
        </div>
    );
};

export default ActionStatusBar;
